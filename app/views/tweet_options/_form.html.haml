= simple_form_for [current_account, tweet_option] do |f|
  = f.error_messages
  = f.label :name
  = f.select :name, @available_objects, {}, {:class => 'select-menu'}
  %br
  
  = f.label :format
  = f.text_field :format, :size => 150
  %br
  = f.button :submit, "Submit!"